apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: life-coach-postgres
  namespace: life-coach
spec:
  serviceName: life-coach-postgres
  replicas: 1
  selector:
    matchLabels:
      app: life-coach-postgres
  template:
    metadata:
      labels:
        app: life-coach-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              value: "lifecoach"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: life-coach-secrets
                  key: DATABASE_URL
            - name: POSTGRES_DB
              value: "life_coach"
            - name: PGDATA
              value: "/var/lib/postgresql/data/pgdata"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - lifecoach
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - lifecoach
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: life-coach-postgres
  namespace: life-coach
spec:
  selector:
    app: life-coach-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
